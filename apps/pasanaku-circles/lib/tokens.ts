/** Same value as `--primary` in `app/globals.css`. */
export const PRIMARY = "#005db4";

/** QR dark modules. Token: `--primary`. */
export const QR_DARK = PRIMARY;

/** QR light modules. Token: `--background` (light). */
export const QR_LIGHT = "#ffffff";
